import Foundation

// Constants namespace 😉, it's not too complicated because we only have 3 items
enum Const {
    static let userMock = UserMock(name: "John", surname: "Snow")
    static let postUserMock = UserMock(name: "Post", surname: "User")
}

